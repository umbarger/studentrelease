<%- model_class = Release -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:c_at) %>:</strong></dt>
  <dd><%= @release.c_at %></dd>
  <dt><strong><%= model_class.human_attribute_name(:educator_id) %>:</strong></dt>
  <dd><%= @release.educator_id %></dd>
</dl>

<%= link_to t('.back', :default => t("helpers.links.back")),
              releases_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_release_path(@release), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              release_path(@release),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
